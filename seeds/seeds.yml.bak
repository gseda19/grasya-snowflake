version: 2

seeds:
  - name: seed_test
    config:
      column_types:
        country_code: varchar(3)
        region_code: varchar(3)
        sub_region_code: varchar(3)
      schema: dbt_gseda
      grants:
        +select:
        - TRANSFORMERS