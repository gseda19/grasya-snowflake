version: 2

models:
  - name: contract_testing
    versions:
      - v: 1
        columns:
          - name: id
            data_type: int
  - name: foo
    columns:
      - name: id
        data_type: int