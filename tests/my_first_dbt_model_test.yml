version: 2

models:
  - name: my_first_dbt_model
    columns:
      - name: id
        tests:
          - not_null:
              description: "id should be not null"
      - name: id2
        tests:
          -  not_null
